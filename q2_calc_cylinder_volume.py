from math import pi

radius = float(input('Please key in the radius of the cyclinder'))
length = float(input('Please key in the length of the cylinder'))

volume = radius * radius * pi * length

print('The volume of the cylinder is:', volume)
